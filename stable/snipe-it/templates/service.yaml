apiVersion: v1
kind: Service
metadata:
  name: {{ template "snipe-it.fullname" . }}
  labels:
    app: {{ template "snipe-it.name" . }}
    chart: {{ template "snipe-it.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "snipe-it.name" . }}
    release: {{ .Release.Name }}
